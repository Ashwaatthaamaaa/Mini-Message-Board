<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Board</title>
</head>
<body>
    <% messages.forEach((message,index) =>{%>
    <div>
        <p><strong><%= message.username %>:</strong> <%= message.text %></p>
        <a href="/message/<%= message.id %>/details">Details</a>
    </div>
    <% })%>
    <h2><a href="/new">ADD NEW Message</a></h2>
</body>
</html>